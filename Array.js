var colors = ["red","yellow","orange"];
console.log(colors[1]);
// Update the value
colors[3] = "dark green";

colors[10] = "violet";
// from 4 to 9 will be empty items
console.log(colors);

var friends = []; // empty array
var friends = new Array(); // empty array

//  We can fill array with all sorts of data

console.log(colors.length) // gives the length of an array
